Source URL: https://developers.google.com/maps/documentation/android-sdk/reference/com/google/android/libraries/maps/model/LatLngBounds.Builder?hl=ko
Title: LatLngBounds.Builder

의견 보내기

LatLngBounds.Builder

컬렉션을 사용해 정리하기

내 환경설정을 기준으로 콘텐츠를 저장하고 분류하세요.

공개 정적 최종 클래스
LatLngBounds.Builder
Object를 확장함

LatLng 포인트 집합을 기반으로 최소 경계를 만들 수 있는 빌더입니다.

공개 생성자 요약
|  | LatLngBounds.Builder() |
| --- | --- |

공개 메서드 요약
| LatLngBounds | build() LatLng 경계를 만듭니다. |
| --- | --- |
| LatLngBounds.Builder | include(LatLng 지점) 경계 빌드에 이 지점을 포함합니다. |

상속받은 메서드 요약

java.lang.Object
클래스에서 상속됨

|  |  |
| --- | --- |
|  |  |
|  |  |
|  |  |
|  |  |
|  |  |
|  |  |
|  |  |
|  |  |

공개 생성자

공개

LatLngBounds.Builder
()

공개 메서드

공개
LatLngBounds [https://developers.google.com/maps/documentation/android-sdk/reference/com/google/android/libraries/maps/model/LatLngBounds?hl=ko]

build
()

LatLng 경계를 만듭니다.

생성 값
| IllegalStateException | 포인트가 포함되지 않은 경우입니다. |
| --- | --- |

public
LatLngBounds.Builder [https://developers.google.com/maps/documentation/android-sdk/reference/com/google/android/libraries/maps/model/LatLngBounds.Builder?hl=ko]

include
(LatLng [https://developers.google.com/maps/documentation/android-sdk/reference/com/google/android/libraries/maps/model/LatLng?hl=ko] 포인트)

경계 빌드에 이 지점을 포함합니다. 이 지점을 포함하도록 경계는 최소한의 방식으로 확장됩니다.
더 정확하게는 동쪽과 서쪽 방향 모두에서 경계를 확장하고 (둘 중 하나는 반대 자오선을 가로지를 수 있음) 둘 중 더 작은 쪽을 선택합니다. 두 방향으로
크기가 동일한 LatLngBounds가 발생하는 경우 동쪽 방향으로
확장됩니다. 예를 들어 점 (0, -179) 및 (1, 179)를 추가하면 경도 180을 가로지르는 경계가 생성됩니다.

매개변수
| point | 경계에 포함할 LatLng입니다. |
| --- | --- |

반환 값
새 점이 추가된 이 빌더 객체입니다.

의견 보내기