Source URL: https://developers.google.com/workspace/gmail/postmaster/guides/verify-domain

이 페이지는 Cloud Translation API [https://cloud.google.com/translate/?hl=ko]를 통해 번역되었습니다.
Switch to English
홈 [https://developers.google.com/?hl=ko]
Google Workspace [https://developers.google.com/workspace?hl=ko]
Gmail [https://developers.google.com/workspace/gmail?hl=ko]
가이드 [https://developers.google.com/workspace/gmail/api/guides?hl=ko]
의견 보내기
인증 도메인 확인
bookmark_border
이 페이지의 내용
특정 도메인 확인
모든 도메인 확인
특정 도메인 또는 Postmaster에 등록한 모든 인증 도메인을 확인할 수 있습니다.
특정 도메인 확인
특정 도메인을 확인하려면 도메인 이름과 함께 domains.get() [https://developers.google.com/workspace/gmail/postmaster/reference/rest/v1/domains/get?hl=ko]을 호출합니다. 다음은 특정 도메인을 확인하는 방법을 보여주는 코드 샘플입니다.
--- 탭: 자바 ---
/**
   * Gets a specific domain registered by the client.
   *
   * @param service Authorized Gmail PostmasterTools API instance.
   * @param domainName The fully qualified domain name.
   * @return The domain
   * @throws IOException
   */
public static Domain getDomain(PostmasterTools service, String domainName) throws IOException {
  String query = String.format("domains/%s", domainName);
  Domain domain = service.domains().get(query).execute();
  System.out.println(domain.toPrettyString());
  return domain;
}

--- 탭: Python ---
"""Gets a specific domain registered by the client.

Args:
service: Authorized Gmail PostmasterTools API instance.
domain_name: The fully qualified domain name.

Returns:
The domain.
"""
def get_domain(service, domain_name):
  try:
      query = 'domains/' + domain_name
      domain = service.domains().get(name=query).execute();
      print(domain)
      return domain
  except errors.HttpError as err:
      print('An error occurred: %s' % err)
모든 도메인 확인
모든 도메인을 확인하려면 domains.list() [https://developers.google.com/workspace/gmail/postmaster/reference/rest/v1/domains/list?hl=ko]을 호출하세요. 다음은 모든 도메인을 확인하는 방법을 보여주는 코드 샘플입니다.
--- 탭: 자바 ---
/**
   * Lists the domains that have been registered by the client.
   *
   * @param service Authorized Gmail PostmasterTools API instance.
   * @return Response message for ListDomains.
   * @throws IOException
   */
public static ListDomainsResponse listDomains(PostmasterTools service) throws IOException {
  ListDomainsResponse listDomainsResponse = service.domains().list().execute();
  for (Domain domain : listDomainsResponse.getDomains()) {
    System.out.println(domain.toPrettyString());
  }
    return listDomainsResponse;
}

--- 탭: Python ---
"""Lists the domains that have been registered by the client.

Args:
service: Authorized Gmail PostmasterTools API instance.

Returns:
Response message for ListDomains.
"""
def list_domains(service):
  try:
      domains = service.domains().list().execute()
      if not domains:
          print('No domains found.')
      else:
          print('Domains:')
          for domain in domains['domains']:
              print(domain)
      return domains
  except errors.HttpError as err:
      print('An error occurred: %s' % err)
의견 보내기